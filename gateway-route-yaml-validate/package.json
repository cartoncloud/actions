{
  "name": "gateway-route-yaml-validate",
  "version": "1.0.0",
  "description": "Validate gateway route YAML artifacts before publishing",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "YAML"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
