{
  "name": "github-trigger-workflows-and-wait",
  "version": "1.0.0",
  "description": "Trigger a list of workflows and wait for their result",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --conditions=import --outdir=dist"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "TypeScript"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@octokit/rest": "^22.0.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.2"
  }
}
